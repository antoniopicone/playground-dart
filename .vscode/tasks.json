{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Launch Dev Server",
            "type": "shell",
            "command": "flutter packages pub global run webdev serve --hostname 0.0.0.0 --auto restart",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}